---
date: 2026-03-31
summary: Environment variables inheritance fix and file path link detection improvements
---

## Bug Fixes

- Fixed an issue where custom shell environment variables (e.g. `$MY_SERVER`) were not available to the AI agent — all user-defined variables are now inherited correctly
- File path detection in chat messages now recognizes more file types and avoids false positives on URLs and numeric values
